flask==3.0.3
flask-cors==4.0.1
opencv-python==4.10.0.84
mediapipe==0.10.14
numpy==1.26.4
face_recognition==1.3.0
# face_recognition depends on dlib, which needs cmake + a C++ toolchain to build:
#   sudo apt-get install -y cmake build-essential
# On Windows, prefer installing a prebuilt dlib wheel matching your Python version.
